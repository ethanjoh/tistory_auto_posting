# 티스토리 자동 포스팅 프로그램 구현 계획

본 계획서는 공식 API가 없는 환경에서 Playwright를 활용해 티스토리 백업 HTML과 이미지를 안전하게 자동 포스팅하는 프로그램을 구축하기 위한 설계 및 구현 일정입니다.

---

## 1. 요구사항 및 완료 기준

### 요구사항
- **세션 유지**: 카카오 로그인 보안 시스템을 우회하기 위해 Playwright의 `Persistent Context` 기능을 활용하여 로그인 세션을 로컬에 저장하고 재사용합니다.
- **백업 파일 파싱**: 로컬 HTML 파일에서 제목, 카테고리, 발행일, 본문 내용, 로컬 이미지 경로를 파싱합니다.
- **이미지 업로드 및 치환**: 
  1. 본문 내 로컬 이미지를 티스토리 에디터에 먼저 업로드합니다.
  2. 에디터에서 생성된 티스토리 이미지 URL을 획득합니다.
  3. 본문 HTML 내의 로컬 이미지 경로를 해당 티스토리 URL로 치환한 뒤 주입합니다.
- **HTML 모드 전환 및 주입**: 에디터를 HTML 모드로 전환하여 가공된 HTML 코드를 주입합니다.
- **인간 모방(Human-like) 및 우회**:
  - 일일 발행 개수 제한 (SQLite DB로 트래킹하여 오늘 발행 성공 횟수가 10개 도달 시 자동 종료).
  - 행위별 랜덤 딜레이 (1~5초) 및 글자 타이핑 시뮬레이션 적용.
  - 글 작성 간 무작위 대기 시간 (5분~15분) 적용.
- **DOM 변경 대응**: 셀렉터를 설정 파일로 분리하여 웹 구조가 변경되더라도 코드 수정 없이 대응 가능하도록 설계.

### 완료 기준
1. `py login.py` 실행 시 브라우저가 열려 사용자가 수동 로그인한 후 세션이 성공적으로 저장되는가?
2. `py publish.py` 실행 시 DB가 연동되어 당일 작성 횟수가 정확히 기록 및 제어되는가?
3. 로컬 HTML 파일 내 제목, 카테고리, 이미지가 완벽히 파싱 및 변환되는가?
4. Playwright가 글쓰기 페이지에 접속하여 제목을 타이핑하고, 로컬 이미지를 에디터에 업로드한 후, HTML 모드로 전환하여 가공된 본문을 주입하고 최종 발행까지 성공하는가?
5. 변경 사항 및 검증 결과가 `CHANGELOG.md`에 정상적으로 기록되는가?

---

## 2. 오픈 질문 및 고려 사항

> [!IMPORTANT]
> **1. 티스토리 에디터 DOM 구조의 최신 상태 확인**
> 티스토리 에디터의 DOM 셀렉터는 변경될 수 있습니다. 본 스크립트 작성 시 로그인 후 글쓰기 페이지(`https://<blog_name>.tistory.com/manage/post`)의 구조를 확인하기 위해 디버깅용 스크립트(`debug_editor.py`)를 먼저 실행하여 실제 DOM 셀렉터를 직접 추출 및 검증할 예정입니다.
> 
> **2. 이미지 업로드 방식 검증**
> 티스토리 신에디터는 파일 드롭 또는 숨겨진 `<input type="file">`에 파일을 주입하는 방식으로 이미지를 첨부합니다. 이 첨부 과정에서 생성된 이미지 URL을 HTML 모드에서 어떻게 매핑할 것인지에 대한 상세한 매핑 로직을 검증할 것입니다.
> 
> **3. 카테고리 매핑**
> HTML 파일에 기록된 카테고리명(예: `해외여행일지/오사카`)이 실제 사용자의 티스토리 블로그에 존재하지 않을 수 있습니다. 
> - 해결방안: 블로그에 해당 카테고리가 존재하지 않는 경우 기본 카테고리로 발행하거나 카테고리 설정을 건너뛰는 예외 처리를 추가합니다.

---

## 3. 제안하는 프로그램 구조 및 파일 목록

### 폴더 구조
```text
tistory_backup/
├── .venv/                      # 파이썬 가상환경
├── user_data/                  # Playwright 브라우저 프로필 (세션 정보 저장)
├── config.json                 # 사용자 설정 (블로그명, 대기시간, 셀렉터 정보 등)
├── database.db                 # SQLite 데이터베이스 (발행 이력 및 오늘 카운트 관리)
├── login.py                    # 최초 로그인 세션 생성 스크립트
├── debug_editor.py             # 에디터 DOM 구조 분석 및 테스트용 스크립트
├── parser_utils.py             # HTML/이미지 파싱 유틸리티
├── publish.py                  # 자동 발행 메인 프로그램
├── CHANGELOG.md                # 변경 기록 파일
└── tistory_automation_proposal.md
```

### 소스코드 명세 (역할 설명 주석 필수 적용)

#### [config.json](file:///c:/Users/ethan/Downloads/tistory_backup/config.json)
- 블로그 이름, 크롬 프로필 경로, 대기 시간 등의 파라미터 및 HTML 모드 전환/발행 버튼의 DOM 셀렉터를 관리합니다.

#### [login.py](file:///c:/Users/ethan/Downloads/tistory_backup/login.py)
- Playwright Persistent Context를 띄워 사용자가 수동 로그인하도록 유도하고, 로그인이 완료되면 세션을 저장하고 브라우저를 종료합니다.

#### [debug_editor.py](file:///c:/Users/ethan/Downloads/tistory_backup/debug_editor.py)
- 저장된 로그인 세션으로 글쓰기 페이지에 접속하여 실제 DOM 구조를 캡처 및 텍스트 형태로 분석해 셀렉터가 유효한지 검증하는 디버깅 유틸리티입니다.

#### [parser_utils.py](file:///c:/Users/ethan/Downloads/tistory_backup/parser_utils.py)
- BeautifulSoup4를 사용하여 HTML 백업 파일에서 제목, 본문, 날짜, 카테고리 및 로컬 이미지 경로를 파싱합니다.

#### [publish.py](file:///c:/Users/ethan/Downloads/tistory_backup/publish.py)
- 전체 프로세스를 조율하는 메인 스크립트입니다. DB 카운트 검사 -> 백업 폴더 대상 수집 -> 순차 업로드 진행 (로그인 세션 로드, 글쓰기 진입, 제목 타이핑, 이미지 업로드, 이미지 링크 매핑, HTML 모드 주입, 카테고리/태그 설정, 발행 및 DB 업데이트, 대기)을 수행합니다.

---

## 4. 검증 계획

### 1단계: 환경 및 세션 검증
- 가상환경 활성화 후 `login.py`를 실행하여 사용자가 로그인을 정상적으로 마치고 세션 파일(`user_data/`)이 올바르게 생성되는지 확인합니다.
- `debug_editor.py`를 실행하여 로그인 상태가 유지된 채 글쓰기 에디터 페이지에 도달하는지 캡처 이미지나 콘솔 로그로 확인합니다.

### 2단계: 파서 검증
- `parser_utils.py`에 테스트 HTML 파일을 인풋으로 주어 제목, 본문, 로컬 이미지 리스트가 정상적으로 추출되는지 단독 테스트를 진행합니다.

### 3단계: 통합 포스팅 검증
- DB에 오늘 카운트를 0으로 설정한 후, 테스트용 HTML 폴더 한 곳을 지정하여 `publish.py`를 실행합니다.
- 실제로 티스토리에 비공개 혹은 보호 모드로 글이 정상 업로드되는지 확인하고, 이미지 엑스박스 여부와 HTML 레이아웃 깨짐 현상을 브라우저로 최종 확인합니다.
- 정상 동작이 검증되면 최종 발행 상태를 '공개'로 전환하여 운영할 수 있도록 지원합니다.
